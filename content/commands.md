---
weight: 10
title: "Stream Commands"
---

# Stream Commands

There are several kind of commands.

## StreamElement Commands

This have no effect on the stream, and are usually either mod commands or commands displaying information.

- `!discord` - Shows discord
- `!sfx` - Shows a list of possibel sound effects
- `!piss` - The piss copypasta
- `!docs` - The docs copypasta

## Standard Overlay Commands

These are commands equal to everyone.
They may cost points to use.

The following table describes the stream commands.
Arguments are listed in the string too: `%command <argument1> <argument2>...`
`<argument?>` means an optional argument.

|command|description|cost|
|---|---|---|
|`%checkin`|Check in to the stream. Can only be used once per overlay restart.|Free|
|`%points <username?>`|Checks how much you have. If specified username, checks how much they have.|Free|
|`%flashbang`|Deploying a flashbang|500|
|`%invest <amount>`|Invests `<amount>` to the stock market|`<amount>`|
|`%uninvest <amount>`|Withdraw `<amount>` from the stock market|`<amount>`|
|`%transfer <username> <amount>`|From your balance, gives `<username>` `<amount>` of points.|`<amount>`|
|`%selfthought <message>`|Say something as if you were vanor. See [docs]({{< relref "overlay/selfthought.md" >}})|5000|


VIPs have an additional set of commands they can run.

|command|description|cost|
|---|---|---|
|`%poll <question>;<duration in seconds>;<option 1>;<option 2>;...;<option n>`|Start a poll|Free|
|`%endpoll`|Ends the current poll|Free|

## VIP Overlay Commands

All VIPs are entitled to one stream command.
They will receive special treatment when they run the command, but normal chatters can also use them for a price.
The VIP entitlements are not stated (because they should know).

|command|description|VIP|normal cost|
|---|---|---|---|
|`%blacksilence`|Does a bunch of silencing. Check [docs]({{ relref "overlay/blacksilence.md" }})|nikita|500|
|`%maxwell`|Spawn a bouncing spinning cats|5kuli|100|
|`%showimage <url or tag> <tag?>`|Shows any image on the screen. If a url is provided, and a tag is provided thereafter, saves it. Requires approval from mod or mayoigo if URL.|mayoigo_QwQ|10000|
|`%playaudio <url or tag> <tag?>`|Plays any audio. If a url is provided, and a tag is provided thereafter, saves it. Requires approval from mod or SpookiestSpooks if URL.|SpookiestSpooks|10000|
|`%goodnightkiss`|Forces vanor to say goodnight to you to proceed with the stream.|pastel8844|5000|
|`%mistake`|a mistake|Mr_Auto|5000|
